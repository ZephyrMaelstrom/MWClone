{
  "name": "crucible-critters",
  "private": true,
  "version": "0.0.1",
  "description": "Crucible Critters — an async collect/transmute/battle game (Monster Warlord lineage)",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build:web": "pnpm -C apps/mobile exec expo export --platform web --output-dir dist && rm -rf packages/server/public && cp -r apps/mobile/dist packages/server/public",
    "start": "pnpm -C packages/server start"
  }
}
